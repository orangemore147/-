name: Crypto slow funding carry wave 3B

on:
  workflow_dispatch:
  push:
    paths:
      - 'wave3b_slow_carry.py'
      - 'wave3_funding_carry.py'
      - 'requirements.txt'
      - '.github/workflows/wave3b_slow_carry.yml'

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Restore funding carry data
        uses: actions/cache@v4
        with:
          path: .cache_wave3
          key: binance-funding-spot-perp-2023-2026-07-v1
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run slow carry scan
        run: python wave3b_slow_carry.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results_wave3b
          if git diff --cached --quiet; then
            echo "No result changes"
          else
            git commit -m "Update slow carry results [skip ci]"
            git push
          fi
      - name: Upload results artifact
        uses: actions/upload-artifact@v4
        with:
          name: crypto-slow-carry-results
          path: results_wave3b/
          if-no-files-found: error
          retention-days: 30
