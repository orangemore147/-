name: Fast crypto 2 percent audit

on:
  push:
    branches: [main]
    paths:
      - 'wave6_crypto_fast.py'
      - '.github/workflows/wave6_crypto_fast.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/cache@v4
        with:
          path: .cache_wave3
          key: binance-funding-spot-perp-2023-2026-07-v1
      - run: python -m pip install -r requirements.txt
      - run: python wave6_crypto_fast.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results_wave6_crypto_fast
          git diff --cached --quiet || git commit -m "Add fast crypto 2 percent audit results"
          git pull --rebase
          git push
      - uses: actions/upload-artifact@v4
        with:
          name: wave6-crypto-fast-results
          path: results_wave6_crypto_fast
